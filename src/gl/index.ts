import {createProgram, createShader, sendUniformData} from "./shader";
import {createVertexArrayBuffer} from "./vao";
import {createCubTexture, createTexture} from "./texture";

export default {
    createShader ,
    createProgram ,
    sendUniformData ,

    createTexture,
    createCubTexture,

    createVertexArrayBuffer,
}
